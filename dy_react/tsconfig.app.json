{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Intentionally minimal; migration tool will read from base config
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx"
  ]
}
